import "@/utils/ws"
// import express from "express"
// import router from "@/html/router"
// import { routeParams } from "@/config"
// const app = express()
// const port = routeParams.port
// const server = app.listen(port)

// const close = () => {
//   server.close(() => {
//     console.log("Express server closed")
//     process.exit()
//   })
// }
// app.use(router)
// process.on("SIGINT", close)
// process.on("SIGTERM", close)

// console.log(`${routeParams.host}:${port}`)
